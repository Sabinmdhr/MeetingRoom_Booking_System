export interface Announcements {
  id?: number;
  title: string;
  message: string;
  pinned: boolean;
  // priorityLevel: string;
  // roleId?: number;
  // groupId?: number;
  // allUser: boolean;
  read?: boolean;
}
