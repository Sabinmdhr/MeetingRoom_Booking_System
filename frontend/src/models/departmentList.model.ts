export interface departmentList {
  departmentName: string;
  id: number;
  description: string;
  status:string;
}

export interface addDepartment {
  departmentName: string;
  description: string;
}